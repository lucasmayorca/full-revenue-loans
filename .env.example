# Root .env.example — copy values to backend/.env and frontend/.env.local
# See backend/.env.example and frontend/.env.local.example for service-specific vars

# GCP Project
GCP_PROJECT_ID=your-project-id

# Demo mode: set to true to skip real Syntage/Google calls
DEMO_MODE=true
